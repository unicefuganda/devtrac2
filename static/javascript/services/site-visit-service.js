angular.module("dashboard")
    .service("siteVisitService", function(jsonService, geonodeService) {
        this.siteVisits = function (location) {
            // geonode?

            // ckan?

            // mongodb?
        };
    });